{
  "name": "client-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "MuhanAI client adapter monorepo — thin patches over official Happy builds, NO fork",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
